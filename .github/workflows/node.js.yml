name: Python CI/CD

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    branches:
      - dev
      - 'flask-demo-app/**/*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r flask-demo-app/requirements.txt

      - name: Build and test with coverage
        run: |
          python flask-demo-app/test_script.py

  sonarcloud-scan:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev')
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '11'

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.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}}
          SONAR_TOKEN: ${{ secrets.sqp_c60e62ddf8a4026e05b2c6f44c3529c7d55ccbfd }}
          SONAR_HOST_URL: ${{ secrets.sonarcloud.io }}

  deploy-staging:
    runs-on: ubuntu-latest
    needs: sonarcloud-scan
    steps:
      - name: Deploy to Staging
        run: |
          # Your actual staging deployment commands here
          echo "Deploying to Staging"

  deploy-prod:
    runs-on: ubuntu-latest
    needs: deploy-staging
    steps:
      # Include steps for manual approval if necessary, using environment protection rules
      - name: Deploy to Production
        run: |
          # Your actual production deployment commands here
          echo "Deploying to Production"
