// jest.setup.js

// Example: Mocking a global function
global.console.log = jest.fn();

// Example: Setting up test data
beforeAll(() => {
  // Setup test data or perform any necessary initialization
});

// Example: Cleaning up after tests
afterAll(() => {
  // Clean up test data or perform any necessary teardown
});
